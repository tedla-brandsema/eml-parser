// Package normalize rewrites raw EML ASTs into smaller, more stable forms.
//
// Normalization operates strictly on raw EML AST after concept expansion.
// It does not depend on the concept dictionary and does not change the parser.
package normalize
